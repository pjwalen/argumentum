{% extends "base.jinja2" %}
{% block content %}
            <div class="row">
                <div class="col-12">
                    <table class="table">
                        <thead>
                            <tr class="row">
                                <th class="col-md-2">Title</th>
                                <th class="col-md-2">Left Opponent</th>
                                <th class="col-md-2">Right Opponent</th>
                                <th class="col-md-6">Description</th>
                            </tr>
                        </thead>
                        <tbody>
                        {% for argument in arguments %}
                            <tr class="row">
                                <td class="col-md-2"><a href="{{url_for('argument_get', argumentid=argument.id)}}">{{argument.title}}</a></td>
                                <td class="col-md-2">{{argument.left_opponent}}</td>
                                <td class="col-md-2">{{argument.right_opponent}}</td>
                                <td class="col-md-6">{{argument.description}}</td>
                            </tr>
                        {% endfor %}
                        <tr class="row">
                            <td class="col-md-2">{{ argumentcreateform.title(class_="form-control mb-2 mr-sm-2", placeholder='Title', form='newArgument') }}</td>
                            <td class="col-md-2">{{ argumentcreateform.left_opponent(class_="form-control mb-2 mr-sm-2", placeholder='Left Opponent', form='newArgument') }}</td>
                            <td class="col-md-2">{{ argumentcreateform.right_opponent(class_="form-control mb-2 mr-sm-2", placeholder="Right Opponent", form='newArgument') }}</td>
                            <td class="col-md-6">{{ argumentcreateform.description(class_="form-control mb-2 mr-sm-2", placeholder="Description", form='newArgument') }}</td>
                        </tr>
                        </tbody>
                    </table>
                    <form method="post" action="{{ url_for('argument_create') }}" id="newArgument">
                        {{ argumentcreateform.csrf_token }}
                        <input type="submit" style="display: none;">
                    </form>
                </div>
            </div>
{% endblock %}